// Example 0: Function completion
public void Function
{
    // Ghost text will suggest implementation
}

// Example 1: Function completion
public static int CalculateFibonacci(int n)
{
    // Ghost text will suggest the Fibonacci implementation
}

// Example 2: Pattern recognition
var numbers = new List<int> { 2, 4, 6, 8 };
// Ghost text will suggest the next numbers in the sequence

// Example 3: String manipulation
// build a function to reverse the words in a sentence
public static string ReverseWords(string sentence)
{
    // Ghost text will suggest the implementation
}

// Example 4: LINQ query
var numbers = new List<int> { 1, 2, 3, 4, 5 };
// Create a list of squares for even numbers only
var squaresOfEvens = 

// Example 5: Class definition
public class Person
{
    public string Name { get; set; }
    public int Age { get; set; }

    public Person(string name, int age)
    {
        Name = name;
        Age = age;
    }
    
    // Ghost text will suggest methods like Introduce()
}

// Example 6: API request handling
public static async Task<string> FetchUserData(int userId)
{
    // Ghost text will suggest implementation using HttpClient
}

// Example 7: Regular expression
// Create a regex pattern to validate email addresses
var emailPattern = 

// Example 8: Dictionary manipulation
var userData = new Dictionary<string, object>
{
    { "name", "John Doe" },
    { "age", 30 },
    { "email", "john@example.com" }
};
// Ghost text will suggest operations on the dictionary

// Example 9: File handling with using statement
public static void ProcessLogFile(string filename)
{
    // Ghost text will suggest reading the file line by line
}

// Example 10: Custom sorting
var students = new[]
{
    new { Name = "Alice", Grade = "A", Score = 95 },
    new { Name = "Bob", Grade = "B", Score = 82 },
    new { Name = "Charlie", Grade = "A", Score = 90 }
};
// Sort students by grade then score in descending order using LINQ
// Ghost text will suggest the OrderBy().ThenByDescending() logic
